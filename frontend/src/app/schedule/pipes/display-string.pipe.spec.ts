import { DisplayStringPipe } from './display-string.pipe';

describe('DisplayStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayStringPipe();
    expect(pipe).toBeTruthy();
  });
});
